You must create a virtual environment to install a self-contained dearimgui-bundle

Run these commands in the terminal:
```
python -m venv .venv
```
```
.venv\Scripts\activate.bat
```
```
pip install -r requirements.txt
```
